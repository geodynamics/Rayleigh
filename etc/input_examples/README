Rayleigh v 0.9.0

This directory contains example input files for Rayleigh.
To use one of these files, copy it to your Rayleigh run directory and rename it to "main_input ."

All files were created before the quantity code change that occurred immediately prior to release.
As such, with the physical controls are all correct, the quantity codes employed in these files are
out of date.  These will be corrected in an upcoming minor-version update.
